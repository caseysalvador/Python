# Modules
import converters
from converters import kg_to_lbs
from Utils import find_max

#print(converters.kg_to_lbs(70))

kg_to_lbs(100)

# we use modules to better our code.

#exercise

print("\n")

numbers = [10, 3, 6, 2]
max = find_max(numbers)
print(max)